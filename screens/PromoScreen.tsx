import React from 'react';
import { Text, View } from 'react-native';

export function PromoScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#FFFFFF',
      }}>
      <Text>✨ 優惠活動/優惠券畫面</Text>
    </View>
  );
}
